## Embedding Model

DocMind uses a 1536-dimensional embedding model.

The database vector column is configured as:

Vector(1536)

The application verifies the embedding dimension before relying on vector storage and retrieval.
